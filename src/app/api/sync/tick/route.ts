/* Phase 1 placeholder route (GET): daily sync-runner check (Q-G). Implementation in later items. */
export async function GET() {
  return Response.json({ error: 'Not implemented' }, { status: 501 })
}
