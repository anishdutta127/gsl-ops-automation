/* Phase 1 placeholder route (POST): PUBLIC; HMAC-verified (D7 refinement). Implementation in later items. */
export async function POST() {
  return Response.json({ error: 'Not implemented' }, { status: 501 })
}
