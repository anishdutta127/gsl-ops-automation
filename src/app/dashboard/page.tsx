import { RoutePlaceholder } from '@/components/ops/RoutePlaceholder'

export default function Page() {
  return <RoutePlaceholder title="Ops at a glance" description="Leadership Console: 5 health tiles + 10 trigger tiles + exception + escalation list. (Item 11)" />
}
