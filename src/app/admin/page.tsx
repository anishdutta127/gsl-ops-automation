import { RoutePlaceholder } from '@/components/ops/RoutePlaceholder'

export default function Page() {
  return <RoutePlaceholder title="Admin" description="Admin index. Redirects to /admin/audit in real surface." />
}
