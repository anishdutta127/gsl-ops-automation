import { RoutePlaceholder } from '@/components/ops/RoutePlaceholder'

export default function Page() {
  return <RoutePlaceholder title="New CcRule" description="Anish-required first 30 days post-launch (step 10 Item 8)." />
}
