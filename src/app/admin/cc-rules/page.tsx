import { RoutePlaceholder } from '@/components/ops/RoutePlaceholder'

export default function Page() {
  return <RoutePlaceholder title="CcRules" description="10 rules with per-rule enabled toggle." />
}
