import { RoutePlaceholder } from '@/components/ops/RoutePlaceholder'

export default function Page() {
  return <RoutePlaceholder title="Dispatch" description="Stages 5-7 dispatch flow + override-gate UI for Leadership." />
}
